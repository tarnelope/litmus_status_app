<div class="container">
  <h1>Litmus Status Page</h1>

  <div class="alert <%= status_class(@current_status) %>">
    <p>Litmus is currently <%= @current_status.current_status %>.</p>
    <p>Last updated: <%= @current_status.created_at.strftime("%I:%M%p%z on %m/%d/%Y") %>.</p>
  </div>

  <div>
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col">Message</th>
          <th scope="col">Timestamp</th>
        </tr>
      </thead>
      <tbody class="table-striped">
        <% @status_messages.each do |status_message| %>
          <tr>
            <td><%=h status_message[:description] %></td>
            <td><%= status_message[:created_at].strftime("%I:%M%p%z on %m/%d/%Y") %></td>
          </tr>
        <% end %>
        <%== pagy_nav(@pagy) %>
      </tbody>
    </table>
  </div>
</div>
